import { Command } from '@/core'

export default new Command({
    nameLocalizations: {
        fr: 'boutiques'
    },
    description: '🛒 Access the server shops',
    descriptionLocalizations: {
        fr: '🛒 Accéder aux boutiques du serveur'
    },
    async onInteraction(interaction) {

    },
    async onMessage(message) {

    }
});