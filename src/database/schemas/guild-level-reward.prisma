model GuildLevelReward {
  guildId String
  guild   Guild  @relation(fields: [guildId], references: [id], onDelete: Cascade)

  atLevel           Int
  guildPointsReward Int?
  roleId            String?
  isStackable       Boolean?

  @@id([guildId, atLevel])
  @@map("guild_level_reward")
}
