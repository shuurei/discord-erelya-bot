model GuildModule {
  guildId String
  guild   Guild  @relation(fields: [guildId], references: [id], onDelete: Cascade)

  name     String
  isActive Boolean @default(false)
  settings Json

  @@id([guildId, name])
  @@map("guild_module")
}
